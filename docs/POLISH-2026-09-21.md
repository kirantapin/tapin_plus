# Landing-page polish — 21 Sep 2026

Sam: "Right now it still feels a bit AI generated, even though it's much better
than I thought it would be. I want this to noticeably feel different and refined."
Two pages: the pitch (`/`) and the Coffeeholics splash (`/coffeeholics`). Planned by
Fable 5.1, implemented by Opus 5 workers, verified by Fable.

This is **refinement, not redesign**. The world stays: Gilroy, the maroon dark field on
the pitch, Coffeeholics' purple on the light splash, the nine tokens, the glass panel.
What changes is **composition** — what gets a box, what sits open on the ground, what
is large, what is small, and how the desktop uses its width.

---

## 1. Diagnosis — what reads as "AI generated"

Measured against the references (Mobbin: Shopify Plus, Railway, Dollar Shave Club,
Superpower, sweetgreen, UGLYCASH) and the craft floor:

1. **Every section is the same rounded panel.** Same 16px radius, same hairline, same
   16px padding, stacked. Nothing sits open on the ground; nothing bleeds. The
   references box at most one thing per page.
2. **The icon-tile + bold + grey-subline row, everywhere.** Splash benefits, pitch
   benefit cards, pop-up rows, modal steps. On the pitch desktop it is *four equal
   cards of icon + heading + text*, which is the textbook tell.
3. **Flat type.** One 32px H1, then everything at 13–17px. The number that *is* the
   product ($5, $4.99, $76.25) is never a display moment. The references put the
   figure at 2–3× the headline.
4. **Desktop is a widened phone.** The splash at 1440 is a 720px column with nothing
   either side; its merchant photograph is a 168px strip. The pitch hero's left column
   holds eleven elements in 440px.
5. **Uppercase tracked eyebrows on every panel** ("EVERY WEEK, NOT ONCE", "WHAT YOU
   GET", "APPLIES TO", "WHAT YOU'D SAVE"). One is a label; four is a tic.
6. **Three stacked full-width pills** in the pitch hero on a phone (Get early access,
   How it works, Sign in). One filled action per view; the rest are links.
7. **Small text below the mobile floor.** `.t-compact` is 13px and carries real
   sentences (seat line, one-time note, the alcohol note). Body is 15.

---

## 2. Shared moves — done by Fable before workers start

In `src/styles/tokens.css` only. Workers must not edit tokens.css or layout.css.

- **Type ramp gains two display steps and a section head:**
  - `.t-hero` — 36px phone / 56px from 1024. 700, −.035em, line 1.02. The page H1.
  - `.t-figure` — 48px phone / 64px from 1024. `--font-heavy`, tabular, −.03em, line
    0.95. **The one display moment per surface**: the dollar figure.
  - `.t-section` — 22px, 700, −.02em, sentence case. Replaces `.t-caption` wherever a
    label *heads a section* rather than a list.
- **Mobile floor raised:** `.t-body` 15→16, `.t-compact` 13→14, `.t-lead` 16→17.
  `.t-caption` stays 11px and is now legal **only** on a list label inside a panel or
  on a chip; never as a section heading.
- Section gap on phone stays `--s-3xl` (40); desktop `--s-4xl` (64) stays.

The rule for workers: **a `.t-caption` above content is a smell. Use `.t-section`.**

---

## 3. Coffeeholics splash — `src/routes/Coffeeholics.tsx`, `src/styles/campaign.css`

Mode: Persuade. Cold Meta traffic, phone first. One primary action.

### Phone (375–1023)

1. **Photo-first hero.** The merchant photograph goes full-bleed directly under the
   announce bar, 260px tall (`object-fit: cover`), with the venue-card veil
   (`linear-gradient(180deg, rgba(18,10,14,.10) 0%, rgba(18,10,14,.72) 100%)`). Over
   it, bottom-left: the collar (52px) + `Coffeeholics` (17px 700 white) + `Coffee ·
   Draper Road` (14px, white at .8). Top-left over the photo: the lockup + PLUS chip in
   white. Delete `.cg-venue` as a plate; it becomes this band.
2. **H1 and lead on the ground, not in a panel.** `.t-hero` 36px, max-width 14ch,
   `text-wrap: balance`. Lead `.t-lead` 17px, max 34ch. 24px above the H1, 12 between.
3. **The three benefits become three figures, not three rows.** A 3-across grid of
   open columns separated by 1px hairlines (`border-left: 1px solid var(--hair)` on
   the 2nd and 3rd). Each column: the figure (`$5`, `15%`, `Points`) at `.t-figure`
   48px (the word "Points" at 28px 700 to sit on the same baseline), then the
   qualifier at 14px ink-3 in ≤3 words per line ("credit every week" / "off, once a
   day" / "on every order"). **No icon tiles.** Delete `.cg-icon`. This is the single
   biggest move on the page.
4. **"Try the first two free today"** becomes `.action.action-ghost` at 48px, full
   width, 16px under the figures. Keep the chevron.
5. **The membership block keeps its panel** — it is the one boxed thing on the page,
   because it is the decision. Inside: heading `Every week, not once` as `.t-section`
   (delete the caption). Sentence at 16px. Then the price as **`.t-figure` 48px** with
   `deposit` as a 14px ink-3 label on the baseline beside it. Seat line 14px. Button.
   Also-at row unchanged.
6. Powered-by, legal links, sticky pair: unchanged behaviour. Sticky pair clearance
   stays conditional.

### Desktop (≥1024) — the part Sam named

1. **Two columns, 55 / 45, 32px gap, max 1120px centred.**
   **Left:** lockup + PLUS; H1 `.t-hero` 56px max 12ch; lead 18px max 40ch; the three
   figures in a row (figure 64px); then the two actions side by side — `Get early
   access` filled, `Try the first two free today` ghost — each `flex: 1`.
   **Right:** the merchant photograph as a tall object, `min-height: 560px`, `border-
   radius: 24px`, `object-fit: cover`, with the same veil and the collar + name +
   street overlaid bottom-left. This is the Dollar Shave Club / Superpower split: the
   argument beside the evidence, each getting half the screen.
2. **The membership band is horizontal, not stacked** (Railway's single-plan band).
   One panel, max 1120, `grid-template-columns: 1.2fr 1fr auto`, 32px gap, aligned
   centre: [`.t-section` heading + sentence] | [`.t-figure` price + deposit + seat
   line] | [button, min 220px, with the also-at marks under it]. Hairline dividers
   between the three cells.
3. Powered-by + legal centred under it, 48px gap. No sticky pair at ≥1024 (the fold
   already shows both actions).

### Spacing

Gutter 24 phone / 48 desktop. Between hero and figures 32; between figures and ghost
action 16; between hero block and membership panel 48 phone / 80 desktop.

---

## 4. Pitch — `src/routes/Pitch.tsx`, `src/styles/pitch.css`, shell components named

Mode: Persuade. Keep the mosaic hero; it is the best thing on the page. Fix what is
around it.

### Phone

1. **Hero stack, reduced.** Keep: lockup, H1 (`.t-hero` 36px), lead (17px), proof
   line (14px), the rail. Price: `$4.99` at `.t-figure` 48px + `deposit` 14px label +
   seat line 14px. **One filled button** (`Get early access`). `How it works` becomes
   a 16px text link with a chevron, right of the price line — not a second pill.
   **Delete the six-collar row** (the rail already shows every venue). **Move `Sign
   in` out of the hero** into the announce bar's right edge as a 12px white text
   control (`SignInBar` renders there; keep its sheet behaviour). Delete the hairline
   and the third button.
2. **Let the mosaic breathe.** `.mosaic::after` stops: `.30→.16` at 0%, `.52→.38` at
   34%, `.84→.80` at 72%. The hero card keeps its glass.
3. **"What you get" is not four boxes.** Panel stays `flush` but loses its caption;
   heading `What you get` as `.t-section`. Content (edit `BenefitCards.tsx`):
   - Lead: `$5` at `.t-figure` 48px, beside it `credit every week` 18px 700, under it
     the condition at 14px. No tile.
   - Then `15% off` and `Points toward rewards` as two open columns with a hairline
     between: label 18px 700, condition 14px. No tiles.
   - `Special offers` as a single 14px line under a hairline.
   The page's benefits now rhyme with the splash's figures.
4. **"Applies to"** (`AppliesTo.tsx`, the `.covers` rules): **drop the plates** —
   revert to bare glyph + word, one inline row of six on the ground with a hairline
   above and 20px between items; heading `.t-section`. The alcohol note stays, at 14px,
   as the row's caption. (The plates were a step toward uniformity, not away.)
5. **Savings panel** keeps its box (it is a control). The figure `$76.25` at
   `.t-figure` 48px. The four spend bands become **one segmented control** — a single
   rounded track with four cells and the chosen cell filled — not a 2×2 of separate
   buttons. Heading `.t-section`.
6. **Guarantee is an open statement, no box.** Hairline above, shield glyph at 28px,
   the sentence at 22px 700 (max 22ch), support link at 14px under. It is the
   signature of the page, not another card.

### Desktop (≥1024)

1. Hero left column: H1 56px, lead 18px, figure 56px, actions as one row. Sign in in
   the announce bar. Rail shows three tiles. The column gets `padding-right: 40px`.
2. **"What you get" as an open 3-column grid**, max 1120: `$5 credit` spans 2 columns
   with the figure at 64px; `15% off` and `Points` take one each; `Special offers` is
   a full-width line under. Hairlines only. **No `.bcard` boxes at any width.**
3. Applies to: the same single inline row, centred to the grid.
4. **Savings and guarantee side by side**: savings panel 60% left, guarantee open
   statement 40% right, aligned top — so the close is one movement rather than two
   stacked panels.

---

## 5. Craft floor — refuse these (both workers)

- No same-size cards of icon + heading + text as page structure. No nested cards.
- No `.t-caption` above a heading or as a section head.
- No third full-width button in one view. One filled action per view.
- No text under 14px except legal links, the PLUS chip, and the corner flags.
- No new colours. Every colour is a token. Coffeeholics purple only via the existing
  `--maroon` redefinition on `[data-surface="cg"]`.
- No `opacity` to dim text; use the ink ramp.
- Keep every existing behaviour: the modals, the sticky pair, the sign-in sheet, the
  rail, the popup, the checkout. Copy is unchanged unless the plan says otherwise.
- Do not edit `tokens.css`, `layout.css`, `light.css`, `reserve.css`, or any
  `shell/*` not named in your section.

## 6. Acceptance — measured, not eyeballed

- 375px: no text node under 14px except the three exceptions; body ≥16; every tappable
  ≥44px tall; `scrollWidth === innerWidth`.
- 1440px: splash photo column ≥560px tall; no content column under 320px; the figure
  is the largest type on the page after the H1.
- Both: `npx tsc --noEmit -p .`, `python3 scripts/guards.py`, and
  `node ~/.claude/skills/impeccable/scripts/detect.mjs --json <files>` return clean.
- Contrast ≥4.5:1 on every text node at both widths (Fable's sweep).

---

## 7. Pitch "What you get" — second pass, 21 Sep evening

Sam, reading §4.3 on a phone: "I really don't like this." Asked what, he named
three things: the giant `$5` beside a small "credit" with the condition hanging
under it; the two-column split with the hairline; and the bare glyph row for
"Applies to" (he had asked on 20 Sep for the merchant pop-up's plated styling on
the main page, and §4.4 overrode that). The big figures elsewhere stay: hero
`$4.99`, savings `$76.25`, the splash strip.

Then: "can you take a look at the meandu website? I like their styling for this
kind of thing. I think it'd be cool if we had photos from the coffeeholics
instagram too."

### What me&u does (meandu.com, "Key features", read 21 Sep)

Each feature is a **photograph card**: a lifestyle photo full-bleed under a
~20px radius, a dark veil from the top, the feature name (~24px 700 white) and
one line under it (~15px white at .85) in the top-left, and a **product
fragment** — a frosted dark chip showing the feature as it looks in the app
("Aperol Spritz $24.00" with a thumbnail) — floating in the lower half. 2×2 on
desktop with 24px gaps; stacked on a phone, each card ~400px tall. No icons,
no hairlines, no figures. The photo is the evidence and the chip is the proof.

### The build

**Section, not panel.** The `Panel className="flush"` around "What you get"
goes at every width; the photo cards are the boxes, and a box around boxes is
the nested-card ban. Heading `What you get` as `.t-section` on the ground,
then the cards, then "Applies to", then the alcohol note.

**Four photo cards** (`BenefitCards`, non-compact path only; `compact` and
`.bcards.is-chips` untouched), one per benefit plus offers, in this order:
`$5 credit`, `15% off`, `Points toward rewards`, `Special offers`.

Each card:
- `.bshot` — `position: relative; overflow: hidden; border-radius: 20px;` the
  photograph as `<img>` `object-fit: cover`, `decoding="async"`,
  `loading="lazy"`, `alt=""`. Veil `::after`:
  `linear-gradient(180deg, rgba(18,10,14,.74) 0%, rgba(18,10,14,.30) 48%, rgba(18,10,14,.58) 100%)`.
- Title top-left, 22px 700 white, `letter-spacing: -.015em`; the label string
  from the record, whole (`$5 credit`, `15% off`, `Points toward rewards`,
  `Special offers`). **No split figure.**
- One line under the title: the record's `detail` (for offers: `Like 50% off
  an entrée at Olaika, on top of the three`), 15px 500, white at .84,
  `max-width: 30ch`.
- **The fragment**: one frosted chip, bottom-left, `rgba(18,10,14,.58)`,
  `backdrop-filter: blur(14px)`, 1px `rgba(255,255,255,.14)`, radius 14,
  padding 10px 14px, two lines: a 13px 500 line at white .72 and a 15px 700
  white line. Contents are illustrative order lines, **never alcohol, never a
  number the model does not hold**:
  - credit: `Latte + croissant · $11.50` / `−$5.00 credit`
  - percent: `Two slices · $9.00` / `−$1.35 (15% off)`
  - points: `Every order` / `Points toward a free one`
  - offers: `Olaika` / `50% off an entrée` (read from the offer record)
  The chip has the benefit's `BenefitIcon` (offers: `NavIcon deals`) at 16px
  before the first line.
- Photos: a `benefitShots` map in `src/model/content.ts` keyed by benefit id →
  venue id, resolved through the existing venue `hero` (so the Instagram
  photos Sam wants later are a one-line swap per card). Never a wordmark
  hero and never a `heroIsBright` one under this veil: credit →
  `coffeeholicsva`, percent → `italianospizza` or `olaika` (whichever is the
  food spread, check the file), points → `themilkparlor`, offers → `olaika`.
  Each image is used once across the four.

**Phone (375–1023):** cards stacked, gap 12. Credit card `height: 320px`;
the other three `height: 240px`. Gutter is the page's 16.

**Desktop (≥1024):** credit card full grid width, `height: 380px`, title 28px;
the other three in one row, `gap: 24px`, `aspect-ratio: 4/3`. Max width the
grid (1280). The credit leads by size — Sam, 12 Sep: "this is the big one".

**Applies to: plates, as asked.** Delete every pitch-scoped override of
`.covers`, `.cover`, `.cover span`, `.covers-head` under
`.column > .panel:has(.bcards)` (phone block ~L1600–1654 and the desktop block
~L2663–2690) so the base plated rules draw it — the same plates the merchant
pop-up draws. Re-scope what survives to the new section class, since the
`:has(.bcards)` panel is gone. Phone: 3×2 plates as the pop-up. Desktop: one
row of six, `grid-template-columns: repeat(6, minmax(0, 132px))`, left-set to
the grid, not stretched across 1280. Label 14px stays. Heading `.t-section`.

**Keep:** the alcohol note under the row, verbatim; `AppliesTo` shared with
`/in`; the sign-in, the sticky pair, everything else on the page.

**Acceptance:** §6 as before, plus: no card title under 22px; chip text ≥13px;
the four photographs are four different files; `/reserve`'s chip list and the
venue pop-up's plates render exactly as before (screenshot both).

---

## 8. Checkout foot — the card, redesigned (21 Sep, late)

Sam, on `/reserve` at the foot: "we should /impeccable redesign this section
of the checkout it's a bit weird. The membership card feels out of place."

### Why it reads wrong

Three surfaces nest: the light sheet, a white `Panel` labelled YOUR CARD, and
the dark maroon card inside it. A dark object on a white plate on a light sheet
is a framed picture, and the uppercase caption above it is the eyebrow tic
§2 removed everywhere else. It sits under the venue rail, so the last thing
before the docked button is a *thing you do not own yet* in a box that says
so. The panel was the answer to "not awkwardly" on 20 Sep; the plate is what
is awkward now.

### The redesign (replaces; product truth kept)

The card is real — it is the object `/in` shows after purchase and the name
typed in the sheet fills it — so it stays. It stops being content and becomes
**the sheet's close**:

- Delete the `Panel label="Your card"` wrapper and its caption. The card sits
  on the sheet's own ground, centred, `max-width: 340px`, after a hairline
  `--s-2xl` below the rail.
- One caption **under** it, 14px `--ink-3`, centred: `Yours from Halloween
  2026` — the date from the model's opening constant, never typed.
- Desktop (≥1024): same object in the `card` grid area, no panel dress.
- The docked `Checkout · $4.99 deposit` bar is unchanged and still clears it.
- Nothing above the rail moves. `TapInCard`'s `name`/`innerRef` contract is
  unchanged (the name field still fills it).

Refused: a second box around it; any uppercase label; moving it back above
the plans (Sam, 20 Sep: bounce).

---

## 9. Checkout — one modal, paged (21 Sep, late)

Sam, on the "Your details" sheet rising over the checkout: "the way this pops
up is strange, I think it'd be a part of the checkout modal, almost like it
swipes to the next screen as if this flow is a part of the same modal. We'd
need a back button as well probably."

### What exists

`ReserveLayer.tsx` draws the checkout sheet (header "Get early access" + X,
`.reserve-sheet`) and `Reserve.tsx` fills it (seat count, plans, benefits,
places, card, docked button). The docked button opens `CheckoutSheet.tsx`, a
SECOND portal'd sheet (`.cs-scrim` + `.cs-sheet`) that walks three states:
`PhoneStep` (name + number, then the code), "Confirm your seat" (Holding for,
the charge rows, the consent, the wallet control — §4's block, which must
stay together), and the receipt ("Your seat is held", then "See your
membership" → `/in`).

### The redesign

One sheet, four pages, moved between horizontally. The second sheet, its
scrim and its own header are deleted; nothing inside the steps changes.

- **Pages**, in `ReserveLayer`: `0 Get early access` (everything Reserve.tsx
  draws today) · `1 Your details` (`PhoneStep`, unchanged) · `2 Confirm your
  seat` (the rows + consent + wallet, unchanged, together) · `3 Your seat is
  held` (the receipt; the "already have a seat" state renders here too).
  `CheckoutSheet` becomes the provider of pages 1–3 rendered inline, not a
  portal; its state machine (`identity`, `subscribed`, `paid`, `onPaid`,
  `onIdentity`) is kept as is.
- **The track**: `.rs-pages` is a row of four `.rs-page` panels, each the
  sheet's width; `transform: translateX(calc(var(--step) * -100%))`, 320ms
  `var(--ease)`. Pages that are not current are `inert` and `aria-hidden`
  and get `visibility:hidden` once the move ends, so nothing off-screen is
  tabbable or read. Each page scrolls on its own; the sheet scrolls to top on
  every step change. Focus goes to the new page's title. Under
  `prefers-reduced-motion` the track jumps.
- **The header** is the one header: the title is the current page's; from
  page 1 on, a 44px `Back` chevron sits at the left edge (page 1 → 0, page 2
  → 1 with the identity kept so PhoneStep is not re-asked, page 3 → none: paid
  has no back and the X reads "Done"). The X stays at the right on every page
  and closes the whole layer as it does now.
- **The docked `Checkout · $4.99 deposit` bar** exists on page 0 only.
- **Desktop (≥1024)**: the same pages inside the centred modal; pages 1–3 set
  their content at `max-width: 560px`, centred, on the sheet's ground.
- **Phone**: the pages are the animation Sam described; no swipe gesture is
  added (a horizontal drag inside a vertical scroller is the ticker's own
  bug from 13 Sep).

Kept verbatim: every string in the three steps, §4's block, the Stripe wiring,
the `data-lit` ramp, the sign-in sheet from the announce bar (it is not part
of this flow and stays a sheet).

Refused: a second scrim; re-asking the number on back; any step between the
rows and the wallet control.

### 9.1 The sign-in sheet pages the same way

Sam: "also the same paged flow for the sign in sheet."

`SignInBar`'s sheet is `PhoneStep` alone, which already has two stages
(`"phone"` → `"code"`, with a "change number" path back). So the paging
mechanism lives ONCE, in `PhoneStep`, and both hosts get it:

- **`PhoneStep`** renders its two stages as a two-page track (the same
  `.rs-pages`/`.rs-page` construction as §9 — extract it as a small shared
  `Pager` in `src/shell/` so the checkout's outer track and this inner one are
  one implementation). It gains `onStage?: (stage) => void` and an imperative
  `back()` via a forwarded ref. Its OTP logic, strings and fields are moved
  into the pages, not rewritten.
- **Sign-in sheet**: title `Sign in` on the phone page, `Enter your code` on
  the code page; the Back chevron appears on the code page and calls
  `PhoneStep.back()` (which is the existing "change number" path); the X
  closes as now. The sheet still slides up from the bottom (§A of the
  callout worker's brief).
- **Checkout, page 1**: hosts `PhoneStep`, so the code screen is a slide
  *within* page 1. The header's title follows `onStage` (`Your details` /
  `Enter your code`), and Back on page 1 goes to the phone page if the code
  page is showing, else to page 0. No number is ever re-asked on Back.

---

## 10. Merchant pop-up — redesigned (21 Sep, late)

Sam, on The Burg's card: "when done can we /impeccable redesign these
merchant page pop ups."

### Why it reads as generated

It is the pre-polish language in miniature: a 168px band (often the
merchant's own wordmark, so The Burg is named three times in 300px — band,
collar, heading), two uppercase tracked eyebrows with rules (WHAT YOU GET
HERE, WORKS ON), three icon-tile + bold + grey-subline rows — the textbook
tell §1.2 named — three tall plates, then a full-width button. Nine objects
in one sheet, all the same weight.

### The redesign (replaces; product truth kept)

Everything is still read from the venue record (`policies`, `venueCovers`,
`liveUrl`, `open`, `brandColor`), the sheet stays light, portalled, closable,
and the one action is still "Order at {venue}" or the "opens here when we
do" line. What changes is the composition, so the card rhymes with the new
splash and the pitch's photo cards:

1. **The photograph is the object.** 200px tall on a phone, 220 in the 460px
   desktop pop-up, `object-fit: cover`, the venue-card veil from the foot.
   Over it, bottom-left: the name (22px 700 white) with the PLUS chip beside
   it, and `category · street` (14px white .84). The collar (40px) sits
   top-left over the photo. The close stays top-right. Under the photo, one
   14px `--ink-3` line: `Open now` / `Closed right now` with a 6px dot.
   No `vp-name` heading below the band, no second statement of the name.
2. **Benefits as figures, not rows** — the splash's `.cg-figures`
   construction scaled to the sheet: three open columns with hairlines,
   `$5` / `15%` / `Points` at 30px 700 (`--font`, tabular), qualifier 14px
   `--ink-3` under each, from the record's `kind`: credit → `a week, on
   $10+ orders`; percent → `off, except alcohol`; points → `on every order`.
   No tiles, no eyebrow. A venue with an offer instead (Italiano's) gets one
   line with the deals glyph: `{offer.label} · members only`, unchanged.
3. **Works on: plates, one row.** Sam likes the plates; keep them, but as one
   row of compact plates (glyph + word inline, 40px tall, `--inner`,
   `--r-control`), wrapping to two rows only if six are present. Heading
   `Works on` as 14px 700 `--ink-2`, sentence case, no rule.
4. **The action** is unchanged.

Sizes: everything ≥14px except the PLUS chip. Contrast over the photo
≥4.5:1 (the veil holds to 40% before ramping, as the pitch cards learned).
Refused: any uppercase eyebrow; icon tiles; a wordmark hero without the
veil; a second button.

The pitch's own plated "Applies to" reads the base `.cover`/`.covers` rules
in pitch.css — the pop-up's compact plates are scoped in venue.css under
`.vp-covers`, so the pitch does not move.

---

## 11. Coffeeholics splash — real items, real savings (22 Sep)

Sam: "make the coffeeholics page look more like the other, like showing items
from their page, and when ordering what they save. /impeccable redesign."

### What exists to build from

`docs/data/menus.json` carries Coffeeholics' real menu (12 items with prices
and photographs under `public/items/coffeeholicsva_*.jpg`), read through
`src/model/menu.ts` (`itemsFor`, `itemNamed`). The pitch's "What you get" is
four `.bshot` photo cards (§7) whose chip is an order line + "You save $X",
derived from the model. The splash today states the three benefits as a
figure strip (`.cg-figures`) and nothing shows what an order looks like.

### The redesign

**One shared card.** Extract the pitch's photo card as `src/shell/PhotoCard.tsx`
(props: `img`, `alt`, `title`, `line`, `chip: {icon, line, figure}`, `lead?`),
rendering exactly the `.bshot` markup pitch.css already styles. `BenefitCards`
uses it and its DOM stays byte-identical (diff the rendered HTML). The splash
uses it too — same classes, same veil, same chip — so the two pages share one
construction rather than two lookalikes.

**On the splash**, between the hero block (H1, lead, figure strip, the try
link) and the membership panel: heading `What you'd save here` as
`.t-section`, then three cards from Coffeeholics' own menu, the item photo as
the ground and the benefit as the title:

| title | line | chip (all from `itemNamed("coffeeholicsva", …)` and `BENEFIT`) |
|---|---|---|
| `$5 credit` | `On a $10+ order, once a week` | `California Club · $12.00` / `You save $5.00` |
| `15% off` | `Everything except alcohol` | `French Onion Steak Melt · $13.00` / `You save $1.95` |
| `Points` | `On every order` | `Cheesecake · $5.50` / `Points toward a free one` |

Figures: `$5` = `BENEFIT.creditUsd`; `$1.95` = price × `BENEFIT.percentOff`;
prices from the record; never typed. If an item is missing from the data the
card is not rendered (no placeholder, no guess). The item's own `desc` is not
shown. The chip's icon is the benefit's `BenefitIcon`.

**Phone:** stacked, credit card 260px, the others 220px (the pitch's sizes),
gap 12, the page's gutter. **Desktop (≥1024):** three across, `aspect-ratio:
4/3`, gap 24, the grid's width; the credit card does not span (on this page
the strip above already leads with it).

**The figure strip stays** in the hero: it is the ad's claim in the first
view; the cards are the proof under it. The membership panel, the sticky
pair, the trial modal and the checkout are unchanged. Copy elsewhere is
unchanged.

**The pop-up's rule holds here too:** these are Coffeeholics' items on
Coffeeholics' page, so the credit line says "once a week", not "at each
place".

Refused: icon tiles; a fourth card; typed prices; any text under 14px but
the chip's first line at 13 (the pitch's own exception).

---

## 12. The photo card — three directions to choose from (22 Sep)

Sam, on the credit card: "I see the thinking behind this, but for both pages
I think it'd be good to make 3 design directions for this container we can
take using /impeccable redesign."

One card (`PhotoCard`, `.bshot`) draws the pitch's four benefits and the
splash's three items. Three directions, built as switchable variants so Sam
compares them live on both pages, then one is kept and the switch removed.
Each is a different world, not a padding change. Same facts on every one:
title (the benefit), line (its condition), the item or order line, the
saving.

**A · Split (photo above, plate below)** — Dollar Shave Club / Superpower.
The photograph fills the top ~58% of the card with no text over it and no
veil (it is finally allowed to be a photograph). Below it a solid plate on
`--card` (pitch: the dark card rung; splash: white) carries the copy: the
title at 20px 700, the line at 14px `--ink-3`, a hairline, then the order
line at 14px `--ink-2` on the left and **the saving as the plate's figure**
at 24px 700 tabular on the right (`You save` at 13px above it). No chip, no
glyph. Phone: 300/260 tall; desktop credit spans, others 4/3.

**B · Order line (the app's own row)** — the most literal "what you'd save
when ordering". A light-or-dark `--card` panel with a 96px rounded (14px)
item thumbnail top-left, the item name (16px 700) and its price (16px 500
tabular, `--ink-2`) beside it exactly as a line in My Spot reads, a hairline,
then the benefit title (18px 700) and its condition (14px), and at the foot a
right-aligned ledger: the price struck? — NO strike-through (a struck price
is a discount claim on the item); instead `You save` 13px over the figure at
22px 700. The photograph is a thumbnail, never a ground, so no veil and no
legibility cost. Phone: auto height (~180); desktop 3-across, equal, credit
does not span.

**C · Poster (the current world, pushed)** — me&u done properly. Full-bleed
photograph; the veil moves to the FOOT (`.82` at 100% → `.0` at 45%), and
all the copy sits bottom-left in one stack: the item line as a 13px caption
(`California Club · $12.00`), the saving as the display line (28px 700
`You save $5.00`), then the benefit title (16px 700) and its condition
(14px, `--ink-1` at .84). No chip, no glyph, three lines of type and a
photograph. Phone 300/260; desktop credit spans at 380, others 4/3.

**The switch.** `?card=split|line|poster` (DEV only, like `?seats=`) sets
`data-card` on `.bshots` / the splash's `.cg-save`; the current card is the
default and stays the default until Sam chooses. All three ship the same
`PhotoCard` markup plus whatever wrapper an option needs; the pitch's default
rendering is byte-identical to today.

**Refused on all three:** a struck-through price, a negative figure, icon
tiles, any text under 13px, maroon ink for the saving (it is an outcome, not
a chosen thing), typed numbers.

**Deliverable:** the three live on both pages, and one contact sheet
(`shots/polish-after/card-directions.png`): six frames, A/B/C × phone
(credit card, 500 wide) and desktop (the row, 1440), labelled.

---

## 13. Checkout at tablet and desktop widths — polish (22 Sep)

Sam, on an iPad-sized frame: "can you take a look at this mobile pop up? The
formatting seems a bit off — use /impeccable polish." (His shot was the
pre-polish build; the defects below are measured on the current branch.)

Measured at 1180×820: the two-column grid is `506px 420px`; the left column
runs to y=1106 and the right (the plans panel alone) ends at 573, so half the
right column is empty ground. The venue rail inside `.rs-places` is 1180px
wide starting at x=−222 — it is sized to the viewport like the page's rail
and draws its tiles across the whole modal under the plans column. At 834
(iPad portrait, single column) the content column is 432px wide in an 834px
sheet, and the rail again spans the full width past it.

### Fixes (reserve.css; Reserve.tsx only if the grid needs it)

1. **The rail is contained.** `.rs-places` clips (`overflow-x: clip`) and its
   `.ticker-rail` is sized to the column, not the viewport: `width: 100%;
   margin-inline: 0; --rail-pad: 0` (the hero rail's own construction in
   pitch.css, `.hero-rail .ticker-rail`). At every width the rail's box equals
   the column's box.
2. **The columns end together.** From 1024 the card-as-close moves to the
   right column under the plans: areas `"earn plans" "includes plans"
   "closing plans" "places card"`, right column `align-content: start`. Left
   ≈ band + includes + guarantee + places; right ≈ plans + card. Verify the
   two columns' bottoms are within 64px at 1024, 1180, 1280, 1440, and the
   card keeps its 340px width and caption.
3. **The single column widens on tablets.** From 640 to 1023 `.rs-modal` is
   `max-width: 560px`, centred (it is 432 today, the phone measure stretched);
   the subtitle band still runs edge to edge; the rail is the column's width.
4. **Spacing on the scale** through the grid: column gap `--s-2xl`, row gap
   `--s-lg`, nothing hand-typed.

Everything else on the sheet stays: the band, the count under the button, the
paged steps, the docked bar (<1024 only). Verify at 375, 640, 834, 1024, 1180,
1280, 1440: no horizontal overflow, rail inside its column, column bottoms
level from 1024, the card at 340 with its caption, nothing under 14px but the
chips.

---

## 14. The pitch goes light below the hero (23 Sep)

Sam: "this all made me realize, I think this entire page needs to go light
mode instead of dark. We can have a dark mode header, but the stuff beneath
needs to be more legible."

### The shape

Two grounds, one edge. The announce bar, the hero band (mosaic, glass panel,
lockup, H1, lead, proof, rail, price, actions) stay exactly as they are: the
dark world is the header. Everything under the hero band — What you get,
Applies to and its note, What you'd save, the guarantee, the legal links —
sits on the **light ramp** (`styles/light.css`: page `#F5F2F3`, card white,
inner `#EFEAEC`, the ink ramp inverted). The Coffeeholics page already lives
on a light ramp; this brings the pitch to the same room, with the hero as
its dark header.

### Build

1. **One wrapper, one attribute.** In `Pitch.tsx` everything after the hero
   band is inside `<div className="pitch-lit" data-lit="">`. `light.css`'s
   `[data-lit]` inversion does the tokens; the wrapper paints its own ground
   (`background: var(--page)`), bleeds to the viewport edges
   (`margin-inline: calc(50% - 50vw)`, padding back with the column's
   gutter) and runs to the page's foot (the body's dark gradient must never
   show below it — measure the last 1px of the page). The edge under the hero
   is straight and unadorned.
2. **The two docked bars follow the ground they float over.** `.top-cta`
   (desktop, appears once the hero is scrolled past) and `.sticky-cta` (phone
   pair) both float over the light region, so both carry `data-lit`: a white
   bar with dark ink, the maroon action unchanged (it is 9.19:1 on white).
   Check their hairline/shadow tokens resolve on the light ramp.
3. **Everything in the region reads through tokens.** Audit every rule
   pitch.css applies inside the region for a literal that assumes the dark
   field — white-alpha hairlines, `rgba(255,255,255,…)`, the glass `.panel`
   backdrop, `--on-maroon` used as "white text", `.bshot` plates (the `lit`
   prop on `PhotoCard` is now redundant on the pitch; leave the prop, stop
   passing it), the savings segmented track (`--press` chosen cell), the
   guarantee shield, `.cover` plates, the alcohol note, the section heads
   (`.t-section` is `--ink-1`, inverts by itself). Where a rule is scoped to
   the pitch and assumes dark, re-express it in tokens or scope a
   `[data-lit]` variant beside it. The desktop close band (`.pitch-decide`,
   `--subtle`) on the light ramp is white on `#F5F2F3`; if that reads as a
   panel-on-panel with the savings panel inside it, use `--inner` for the
   band or drop the band's fill.
4. **The photographs keep their edges.** Cards on the light ground get the
   light ramp's `--hair` and `--rest` shadow (light.css defines both), so a
   white plate on a `#F5F2F3` ground still separates.
5. **Nothing above the hero's foot changes**, and `/coffeeholics`, the
   pop-up, the checkout, the sign-in sheet are untouched.

### Acceptance

- 375 and 1440: every text node in the region ≥4.5:1 on its actual ground
  (the sweep in `scratchpad/accept.js` walks ancestors for the painted
  background); no literal colour outside tokens in any rule the region uses
  (grep the diff); nothing under 14px but the chips; no horizontal overflow;
  the docked bars white over the light region and hidden/absent over the
  hero as before; the page's last pixel row is `--page` light, not the dark
  gradient.
- The hero band's rendered HTML and its computed styles are unchanged.

---

## 15. Checkout — the left column speaks the new language (23 Sep)

Sam, on the desktop checkout: "/impeccable redesign this."

### Why it reads as the old build

The plans column (tiles, terms, button, count, the card as the close) is
current. The left column is not: `WHAT YOU GET` and `WHERE IT WORKS` are the
uppercase tracked eyebrows §2 removed page-wide; the benefits are four
icon-tile + bold + grey rows — §1.2's tell, the construction every other
surface has already replaced; the guarantee is a shield-in-a-tile inside a
panel; the rail's tiles are 3:4 posters cut at the column's edge. Three
panels of the same weight, stacked, on the one surface that takes money.

### The redesign (replaces; product truth kept)

Every string still comes from `benefits`, `offers`, `guarantee`,
`GUARANTEE_CONTACT`, `venues`; §4's block on page 2 is untouched; the
right column is untouched.

1. **One figure strip, shared.** Extract the three-figure construction
   (`$5` / `15%` / `1×` with a four-word qualifier each, hairlines between,
   one row) into `src/shell/BenefitFigures.tsx` with its rules in a new
   `src/styles/figures.css` (imported in `main.tsx` after `pitch.css`). The
   splash's `.cg-figures` and the pop-up's `.vp-figures` become uses of it
   and must render measurably the same as today (figure size, baselines,
   qualifier line counts, hairline heights at 375 and 1440). The checkout's
   "What you get" becomes: heading `What you get` (18px 700 `--ink-1`,
   sentence case, no rule), the strip at the pop-up's 30px size, then
   `Special offers` as the pop-up's one offer line (`Like 50% off an entrée
   at Olaika, on top of the three` — the existing string), 14px.
   Qualifiers for the checkout: credit → `a week, on $10+ orders`; percent →
   `off, except alcohol`; points → `on every order` (the pop-up's, since
   this is the network-wide sheet, not one shop's page).
2. **The guarantee is an open statement**, as on the pitch: shield glyph
   24px `--ink-2`, the sentence at 18px 700 (max 24ch), `support@tapin.app`
   14px under it. No panel, no tile. A hairline above it.
3. **Where it works** → heading `Where it works` (same head style), the
   rail with the hero rail's square tiles (160 at ≥1024, the phone's own
   width below), contained to the column as §13 made it.
4. **No panel around any of the three.** The left column is three open
   blocks separated by `--s-2xl`, under the subtitle band. The band keeps
   its white ground; the blocks sit on the sheet's ground.
5. **Phone order** unchanged: band, plans, benefits, guarantee, rail, card.

### Acceptance

- 375 and 1440: zero uppercase eyebrows on the sheet (the `Coming soon`
  corner flag and PLUS are chips, allowed); zero icon tiles in the left
  column; the strip's three figures one row at 375; nothing under 14px but
  the chips; contrast ≥4.5:1; no horizontal overflow; the rail inside its
  column; the splash's and pop-up's strips byte-identical in rendered
  geometry to before the extraction.
- From 1024 the columns' bottoms within 96px (the right column is the plans
  + the card; the left is lighter now, so it may end first).

---

## 16. Checkout — a review sheet, not a landing page (23 Sep)

Sam: "the checkout page needs some more work, can you look at mobbin to
take inspiration and use /impeccable to design."

### What the references do (Mobbin, read 23 Sep)

Cash App "Review your plan", CLEAR+, Panera Sip Club, Epidemic Sound,
Shopify "Start for free, stay for $1", Fresha "Review and checkout", Future
Pro. Every one is a **review sheet**: one column, a ledger of what you pay
and when (Today / next charge / always), the plan as one line, what's
included as a short checklist, a consent or guarantee line, one button that
names the amount. None of them has two columns of marketing panels. Shopify
states the money as a schedule (`Today · 3 days free`, `Jul 3 · $1/mo`,
`Always · cancel anytime`); Fresha leads with the venue as a photo row and
docks "1 membership · $268 · Pay now".

Mode: **Operate**. The reader is deciding whether to pay $4.99; scanability
outranks expression. The pitch and the splash persuade; this sheet confirms.

### The redesign (replaces; product truth kept)

Every string still comes from the model (`PLANS`, `monthlyToday`,
`standardAfter`, `lockedRateLine`, `launchWindow`, `benefits`, `venues`,
`guarantee`, `GUARANTEE_CONTACT`, `seatsLeft`); §4's block on page 2 and
the paged steps are untouched; the docked bar below 1024 and the desktop
`.top-cta` are untouched.

**One column, at every width.** `.reserve-sheet` is `width: min(640px,
100vw − 64px)` from 640 up (centred, the existing pop-in), full-height on a
phone as now. The two-column grid and its areas go. Page 0's content is a
560px measure inside it.

**Page 0, top to bottom:**

1. **The plan tiles** (Early Bird Deposit / Standard · Coming soon) stay as
   they are — they are the one choice.
2. **The schedule** replaces the subtitle band and the two prose paragraphs:
   an open ledger, hairlines between rows, label left (14px `--ink-3`),
   figure right (16px 700 tabular `--ink-1`), a 14px `--ink-2` note under
   each label where one exists. Rows, from the model:
   - `Today` · `$4.99` · `Early Bird deposit, counts toward your first month`
   - `Your first $10+ order` · `+$5 credit` · `more than the deposit` (the
     earn-back line, as a row; keep its `FOUNDING_OPEN && monthlyToday <
     creditUsd` gate; a `+` on a credit is not a minus on a price)
   - `Halloween 2026` · `$4.99 a month` · `your first month, then every month`
   - `Always` · `$4.99` · `never goes up while you stay a member`
   - `Before we open` · `Full refund` · `no reason needed`
   The figures column is one width so the amounts align.
3. **The button** — `Checkout · $4.99 deposit` (the docked bar's own label,
   so page 0's control and the bar say the same thing) — then the count
   under it exactly as it is (line, meter, close date).
4. **What you get** — heading 18px 700, then a four-line checklist (a 16px
   check glyph in `--ink-2`, the line at 15px `--ink-2`): `$5 credit every
   week` · `15% off, except alcohol` · `1× points, toward free items` ·
   `Special offers from the places`. Strings assembled from `benefits`
   labels and the pop-up's qualifiers; no figure strip here (it is a
   marketing device; this is a receipt).
5. **Where it works** — one row: the six collars (the splash's `.cg-also`
   construction) and `Coffeeholics, The Burg, The Milk Parlor, Olaika,
   Sweetopia and Italiano's` at 14px from `venues`. The rail leaves the
   checkout (it stays on the pitch).
6. **The guarantee** — one line with the shield glyph at 20px:
   `Save at least what you pay, or we refund the difference.` 15px 700,
   `support@tapin.app` 14px after it. No panel.
7. **The card** — the close, as §8: hairline, 340px, `Yours from Halloween
   2026`.

Spacing: `--s-2xl` between blocks, `--s-lg` inside them; every text ≥14px;
tokens only; the sheet's own `[data-lit]` ramp.

**Refused:** a second column; any panel around 2, 4, 5 or 6; the figure
strip; the rail; uppercase eyebrows; a struck-through price; a minus sign;
a promo-code field (there is no promo); "Subtotal / Tax / Total" rows
(there is one charge and no tax line in the model).

### Acceptance

375, 640, 834, 1180, 1440: one column; sheet ≤640 from 640 up; ledger
figures right-aligned on one edge; nothing under 14px but the chips;
contrast ≥4.5:1; no overflow; the paged steps, the scrim close, the docked
bar and `.top-cta` all as before; every figure equal to the model's.

---

## 17. Coffeeholics desktop — the hero earns its motion (23 Sep)

Sam: "the desktop version of this page needs /impeccable redesign, and use
/impeccable animate to bring it to life. I think it'd be neat if the header
had some kind of perpetual animation that showed how using the deals can
pocket savings for consumers over time — like how much they get back on the
$4.99 they spend on the membership."

### Diagnosis

The desktop hero is a correct split (argument left, evidence right) and
nothing in it moves or argues over time. The photograph is a static card;
the three figures state the claim; the cards below prove it on single
orders. What no surface shows is the thing the membership actually is: the
same $5 arriving every week against one $4.99. That is a sequence, and a
sequence is the one thing on this page that has earned animation.

### The redesign

**The photograph becomes a stage.** The right column keeps the merchant
photograph (`.cg-band`, its veil, collar, name and street) and carries one
object over it: **the month ledger**, a white card (`--card`, radius 20,
`--float` shadow, 360px wide, bottom-right of the photograph with 24px
insets; on the pitch's ramp rules, white over a photograph is the plate
§12 A chose). Nothing else on the hero changes: lockup, H1, lead, the
figure strip, the two actions.

**The month ledger** (`src/shell/SavingsLedger.tsx`, styles in
`campaign.css` under `.ml-*`): a receipt that fills itself.
- Head: `One month at Coffeeholics` 14px 700 `--ink-2`.
- Row 0: `Early Bird membership` · `$4.99` (from `monthlyToday`).
- Rows 1–4: `Week 1 · California Club`, `Week 2 · Chipotle Turkey Melt`,
  `Week 3 · French Onion Steak Melt`, `Week 4 · Cappuccino + Croissant +
  Cookie` — four real Coffeeholics baskets from `itemsFor("coffeeholicsva")`
  that each clear `BENEFIT.creditMinUsd` (the fourth is three items summed;
  drop any basket whose items are missing or under the floor). Each row's
  figure: `+$5.00 credit` (`min(BENEFIT.creditUsd, price)`), 15px 700,
  tabular. The 15% and points are NOT counted here: the credit is the one
  weekly figure the model states plainly, and a ledger that stays inside
  what is certain is the one a reader trusts.
- Foot, under a hairline: `You're ahead` · **`$15.01`** at 24px 700 — the
  four credits less the membership, computed in cents, never typed.
- Every string ≥14px; ink ramp only; the credits are `--ink-1`, not green,
  not maroon (an outcome, not a chosen thing).

**The motion** (`/impeccable animate`; read `reference/animate.md`):
- The loop is the argument, so it runs on its own, forever, and never asks
  for a hover. One cycle ≈ 11s: rows 1–4 arrive one at a time, 900ms apart,
  each entering with a 12px rise and opacity 0→1 over 280ms on the house
  ease; as each lands, the foot's figure counts from its previous value to
  the new one over 400ms (tabular numerals, so nothing shifts; start at
  `−$4.99`, so the first credit takes it through zero — that crossing IS the
  message). After row 4 the foot holds 3.2s, then the card's rows fade out
  together over 240ms and the cycle restarts with row 0 already in place.
- `transform` and `opacity` only; no height animation (rows are laid out at
  full size from the start and revealed; the card never changes size). The
  loop is a single `requestAnimationFrame`/timeout schedule that clears on
  unmount and pauses when the tab is hidden (`document.visibilityState`).
- `prefers-reduced-motion: reduce`: no loop — the ledger renders complete
  and still, with the foot at `$15.01`. Same rule under `data-still` if the
  page ever gets one.
- Nothing else on the page animates. The figure strip, the cards and the
  band stay still; one moving object per screen.

**Phone (<1024)**: the same ledger, complete and still (no loop — the
photograph band is 260px and the card would cover it), placed as a full-
width card between the try link and `What you'd save here`. Heading and
rows as above.

**Refused:** confetti, sparklines, animated numbers anywhere but the foot;
any figure the model does not hold; a hover-triggered loop; more than one
animated object in the hero; text over the photograph other than the
existing name/street; the strip or the cards changing.

### Acceptance

- 1440×900: the ledger sits inside the photograph's box with 24px insets;
  its rows arrive on the schedule above (measure two consecutive row
  entrances ≈900ms apart via `getAnimations()` timings or timestamps);
  the foot reads `$15.01` at the end of a cycle; the loop restarts; CPU
  idle between steps (no continuous rAF); reduced-motion renders complete
  and still.
- 375: the ledger card below the try link, complete, no motion, no text
  under 14px, contrast ≥4.5:1, no overflow.
- Every figure equal to the model's; the pitch untouched.

---

## 18. Checkout — the included panel, and desktop without a scroll (23 Sep)

Sam, three notes on one screen:
1. "applies to cards should be the same white background as the other
   containers" — done directly in pitch.css (`.pitch-lit .cover` = `--card`
   + hairline + `--rest`).
2. On the trial modal's call-out (the `--inner` panel: bold heading, the
   collar stack beside "Also at …", the price line, the button): "I like how
   you formatted this, maybe we use the same on the checkout flow or
   something similar."
3. "checkout on desktop should be wider so I don't need to scroll through
   anything."

### The included panel (phone and desktop)

Page 0's three open blocks after the count — the checklist, the collar
line, the guarantee line — become ONE panel in the trial modal's
construction (`.ct-more` in campaign.css is the reference: `--inner`
ground, radius 20, 20px padding, 12px between children):
- Heading, 18px 700 `--ink-1`: `Every week, at every location` — a
  statement of the modal's own question, since at checkout it has been
  answered. Words, not figures.
- The four-line checklist as it is (check glyph, 15px `--ink-2`).
- The collar row in the modal's two-column form: the six collars stacked
  left (`.cg-also-marks`), and to the right, 14px `--ink-2`:
  `At Coffeeholics, The Burg, The Milk Parlor, Olaika, Sweetopia and
  Italiano's Pizza.` from `venues`.
- A hairline, then the guarantee line (shield 20px, the sentence 15px 700,
  `support@tapin.app` 14px).
No button inside it: the action stays with the ledger. No price line inside
it: the tiles and the ledger carry the money.

### Desktop without a scroll (≥1024)

Two columns again, because the reader's screen has the width and Sam does
not want to scroll: the sheet is `min(1040px, 100vw − 64px)`, height fitted
to content, capped at `100vh − 48px`; columns `1fr 1fr`, gap 32, both
`align-content: start`.
- Left: the plan tiles, the schedule ledger, the button, the count.
- Right: the included panel, then the card as the close (hairline, 340,
  caption).
Target: at 1440×900 and 1180×820 the sheet's scroller has
`scrollHeight ≤ clientHeight` (nothing to scroll). If it does not fit at
820, tighten the ledger's row padding (`--s-md` → `--s-sm`) before anything
else; do not shrink type.

Below 1024: one column in the review-sheet order, with the panel in place
of the three blocks; the sheet as it is.

Refused: a button or a price inside the panel; a third column; any figure
not the model's; text under 14px but the chips.

### Acceptance

375, 834, 1180×820, 1440×900: one column below 1024 and two from it; no
internal scroll at the two desktop sizes (report `scrollHeight` vs
`clientHeight`); the panel's ground `--inner` on the sheet's `--page`;
contrast ≥4.5:1; the paged steps, the scrim close, the docked bar as
before; the pitch, the splash, the trial modal unchanged.

---

## 19. The pitch's desktop close — one composition (23 Sep)

Sam, on the pitch at 1440, the region from "Applies to" to the legal links:
"use /impeccable redesign for this desktop section."

### Diagnosis

Three objects at three weights, none aware of the others: a row of six
plates across the left three-quarters of the grid with a bare sentence
under it; a white savings panel whose control is 60px tall inside a 490px
box; the guarantee floating in the right column over 300px of empty
ground. The close of the page is where the argument lands, and it lands in
three places. (The phone stacks these and reads fine; this is a desktop
composition problem.)

### The redesign (desktop ≥1024; phone order unchanged)

**The close is one two-column band.** `.pitch-decide` becomes a grid,
`1.15fr 1fr`, column gap 48, row gap 32, areas `"save scope" "save
guarantee"`, rows `auto 1fr`:
- **Left (`save`)**: the savings panel as it is (`.spendq`: heading,
  `$76.25 a month`, the segmented spend track, the estimate line, the
  "Where it comes from" drill, "How it works").
- **Right, top (`scope`)**: `Applies to` moves here from the "What you get"
  section — heading `.t-section` with NO rule after it, the six plates as
  a 3×2 grid (`repeat(3, 1fr)`, gap 12, the white plates §18 made), the
  alcohol note under at 14px `--ink-3`.
- **Right, bottom (`guarantee`)**: the open guarantee statement as it is,
  `align-self: end`, so its foot is level with the panel's foot.
The two columns' bottoms within 24px at 1440 and 1280; the band's top
edge is the "What you get" cards' foot plus `--s-4xl`.

**How it moves without changing the phone**: in `Pitch.tsx`, `AppliesTo` and
the alcohol note leave the `pitch-get` section and become the FIRST child
of `.pitch-decide` as `<div className="pitch-scope">`. DOM order is then
cards → scope → savings → guarantee, which is exactly today's phone order,
so below 1024 nothing changes but the plates' colour (§18). The
`.covers-head::after` rule is removed at every width (a head that stands
over its row carries no rule; the plates are the object).

**Refused:** a panel around the scope block or the guarantee; the plates
back to grey; a third column; the savings panel losing its box (it is a
control); any copy change.

### Acceptance

1440 and 1280: two columns, column bottoms within 24px, plates 3×2 white
with hairline + rest, no rule after "Applies to", contrast ≥4.5:1, nothing
under 14px but the chips, no overflow. 375: the section order is cards →
Applies to → What you'd save → guarantee (list the headings in DOM order),
plates 3×2 as before, the hero and everything above untouched (fingerprint
the hero band).

---

## 20. Coffeeholics hero — the ledger refined, the actions quieted (23 Sep)

Sam, on the desktop hero: "I like where you're headed with this, and I
like the animation, but I think it could be touched up a bit or even
redesigned using /impeccable redesign. Also don't like the CTA buttons here
— everything below looks great though — the second screenshot [the
membership band: `Every week, not once` | `$4.99 deposit` | button + also-at]
looks amazing."

### Diagnosis

The ledger is right in idea and plain in form: nine lines of the same 15px
weight, "+$5.00 credit" four times, a foot that is only larger. It reads as
a table, not as a piece of the app. The two hero actions are two equal
slabs, `flex: 1 1 50%` each: a 269px purple bar beside a 268px grey bar,
where the band Sam likes sizes its one button to its words and puts the
second thing beside it as text.

### The ledger, redesigned (`SavingsLedger.tsx`, `.ml-*`)

Same facts, same loop, a receipt from the app rather than a table:
- Card 340 wide (`min(340px, 100% − 48px)`), 20px padding, `--card`,
  radius 20, `--float`. Bottom-right of the photograph, 24px insets, as now.
- Head: `One month at Coffeeholics` 14px 600 `--ink-3`.
- Row 0: `Early Bird membership` 15px `--ink-2` · `$4.99` 15px 700
  `--ink-1` tabular. A hairline under it.
- Week rows: a **32px item thumbnail** (radius 8, the basket's first
  item's own `img`, `object-fit: cover`) at the left; beside it two lines,
  the basket's name 15px 600 `--ink-1` and `Week 1` 14px `--ink-3` under
  it; at the right `+$5.00` 15px 700 `--ink-1` tabular (the word "credit"
  once, in the head — `One month at Coffeeholics · $5 credit a week` is
  too long; put it as the row-0 hairline's caption instead: a 14px
  `--ink-3` line `Then $5 credit, every week you order` between row 0 and
  week 1 — assembled from `BENEFIT.creditUsd`). Rows 12px apart.
- Foot, under a hairline: `You're ahead` 15px 600 `--ink-2` · the figure
  **28px 700 tabular**. While the figure is negative it is `--ink-3` and
  the label is hidden; at the zero crossing the figure becomes `--ink-1`
  and the label fades in (200ms). No colour but the ink ramp; the crossing
  is told by weight of ink, not by green.
- Motion as §17, with one added beat: 1.2s on `−$4.99` before week 1
  arrives (the cycle becomes ≈9s). Thumbnails ride with their rows (the
  row is one element; no separate animation).
- Phone: the same card, complete and still, under the try link, as now.

### The actions (desktop ≥1024)

The band's construction: `Get early access` as `.action` sized to its
words (`display: inline-flex; width: auto; min-width: 220px; padding:
0 32px; height: 48px`), and `Try the first two free today ›` as the phone's
own 16px 700 text link with the chevron, beside it, 24px gap, centred on
the button's axis. Delete the desktop ghost-pill rules for `.cg-try`; the
`.cg-acts` row is `display: flex; align-items: center; gap: 24px`. The
docked pair below 1024 is unchanged. Confirm the hero's foot still sits
inside a 900px viewport at 1440 (it should rise, not fall).

Refused: green or maroon on the figure; a third action; a progress bar; any
figure not the model's; text under 14px; a thumbnail that is not the item's
own image (a basket with no image drops its thumbnail, not its row).

### Acceptance

1440×900: card 340 wide inside the photograph with 24px insets; four
thumbnails from `/items/coffeeholicsva_*.jpg`; the foot `--ink-3` at
`−$4.99` and `--ink-1` at `$15.01` with the label visible only when
non-negative; the loop timings as §17 plus the 1.2s opening beat; idle
between steps; reduced motion complete and still; the actions one filled
button + one text link on one row, no grey slab; the hero's foot ≤ 900.
375: card complete and still, thumbnails 32px, nothing under 14px,
contrast ≥4.5:1, no overflow; the docked pair unchanged.

---

## 21. The calendar on the pitch (23 Sep)

Sam, on the canvas's C · Calendar direction: "I like the calendar graphic a
lot, wondering if you could implement that on the main LP." And, on the
same graphic: "include offerings from other locations like cover at milk
parlor or line skips on the weekend, drinks at the burg or olaika."

### What it is

A month on the membership, as a calendar that fills itself: four week rows
× seven days; one order at each place each week appears as the item's own
image in its day cell with a `+$5` chip; a line under the grid names the
latest order; a foot totals the membership against the credit earned and
counts "You're ahead" up from `−$4.99`. It is the pitch's own claim ("$5
credit every week, at every place you already go") shown happening.

### Where it goes

- **Desktop (≥1024)**: over the hero's mosaic, as the Coffeeholics page
  puts its receipt over the photograph: a white card, 460 wide, `--card`,
  radius 24, `--float`, bottom-right of `.mosaic` with 32px insets. The
  mosaic's veil holds behind it. The loop runs here.
- **Phone (<1024)**: the first block of the light region, before "What you
  get": the same card full-width, complete and still (no loop), heading and
  all. `.pitch-lit`'s spacing applies.

### The card

`src/shell/MonthCalendar.tsx`, styles `.mc-*` in `pitch.css`:
- Head: `Your first month on the membership` 17px 700 `--ink-1`; under it
  `$5 credit at every place, every week` 14px `--ink-3`. No photo header on
  the pitch (the mosaic is the photograph).
- The grid: a `56px repeat(7, 1fr)` grid, 8px gaps; day-of-week letters
  12px→**14px** 600 `--ink-4` centred (14 is the floor here); week labels
  `Week 1`…`Week 4` 14px 600 `--ink-3`; day cells square (`aspect-ratio:
  1`), radius 12, `--inner`; an order cell shows the item image
  (`object-fit: cover`) with a `+$5` chip (11px 700 white on `--maroon`,
  bottom-right corner). Cells enter with opacity 0→1 and scale .92→1 over
  280ms on the house ease.
- Latest line: 14px, `<b>` for the item, then `at {venue} · +$5.00`; empty
  state before the first order: `Order once a week at each place`.
- Foot under a hairline: `Early Bird membership · $4.99`, `Credit earned,
  four places · $X`, then `You're ahead` + the figure at 32px 700 tabular,
  `--ink-3` while ≤ 0 and `--ink-1` above, the label fading in at the
  crossing.

### The orders — every one from the records

`src/model/month.ts` builds the month from `itemsFor(venueId)` and
`BENEFIT`: an order is `{ week, day, venueId, item }` where the item is a
real menu item whose price clears `BENEFIT.creditMinUsd`; its credit is
`min(BENEFIT.creditUsd, price)`; an order whose item is missing from the
data or under the floor is dropped and the totals follow. The month:

| week | day | venue | item (from menus.json) |
|---|---|---|---|
| 1 | Tue | Coffeeholics | California Club |
| 1 | Sat | The Milk Parlor | Munchie Sampler |
| 2 | Thu | Coffeeholics | Chipotle Turkey Melt |
| 2 | Fri | Olaika | Olaika Burger — Peruvian Style |
| 2 | Sat | The Milk Parlor | Loaded Fries Basket |
| 3 | Mon | Coffeeholics | French Onion Steak Melt |
| 3 | Fri | The Burg | Lomo Saltado — Peru |
| 4 | Wed | Coffeeholics | Cappuccino + Butter Croissant + Baked Cookie |
| 4 | Fri | Olaika | Classic Burger |
| 4 | Sat | The Milk Parlor | The Spicy Redneck |

Ten orders → credit $50.00 → ahead $45.01, all computed in cents. Sam's
cover / line-skip / drinks rows are the intent, and the model has no price
for any of them yet; when the pass and drink records land, a row is one
line here. The cell image is the item's own `img`; if an item has none, the
venue's logo (`venue.logo`) stands in, so The Milk Parlor's and The Burg's
cells still say whose night it is.

### Motion (as §17/§20)

1.2s on `−$4.99`, then one order every 900ms in chronological order, the
foot counting 400ms to each new total, 3.2s hold, 240ms fade, restart
(≈14s). Transform/opacity only; one timer schedule; idle between steps;
paused when hidden or off screen; cleared on unmount; complete and still
under `prefers-reduced-motion` and on phones.

### Acceptance

1440×900 and 1280×800: the card inside the mosaic's box with 32px insets,
not covering the hero copy; ten cells fill on the schedule; the foot ends
at `$45.01`; contrast ≥4.5:1 on the card; nothing under 14px but the `+$5`
chip; the hero copy column untouched (fingerprint `.hero` HTML). 375: the
card first in the light region, complete and still, no overflow, nothing
under 14px but the chip. The Coffeeholics page unchanged.

### 21.1 — placement, marks, desktop only (23 Sep, later)

Sam, at 1440: "the placement here doesn't sit with me quite right — and
maybe it should be the merchant logos. I think this would be only on
desktop." So: the card is centred in the mosaic's box on both axes (480
wide, `inset: 0; margin: auto`), each order cell is the venue's own collar
(`venue.logo`, `logoField`, `brandColor`) rather than the dish, and the
card does not render below 1024 — the light region opens on "What you get"
again. The orders, the price gate and the arithmetic are unchanged.

---

## 22. The month, quieter (23 Sep, late)

Sam, on the pitch's calendar (and "for both pages"): "love this in theory,
but it's just slightly too busy still."

### What is busy

Twenty-eight filled day tiles, ten marks each wearing a brand ring and a
`+$5` chip, a seven-letter header, four week labels, a two-line latest
line, a three-row foot. Fifty-odd objects to say one thing: ten orders, one
figure. The receipt on the splash has the same problem in miniature: a
thumbnail, a name, a week label and a figure per row, a caption, a head and
a three-line foot.

### The calendar (pitch, desktop)

Everything stays where it is; the count of objects halves:
1. **Empty days are not tiles.** A day with no order is a 6px dot in
   `--inner` centred in its cell (the cell itself has no fill); the seven
   weekday letters stay at 12px `--ink-4`, the four week labels stay. The
   grid's rhythm survives as a field of dots; the orders are the only
   objects.
2. **The marks lose the chip.** The head already says every order is $5;
   ten chips repeat it ten times. The mark is the collar alone — 2px brand
   band, no outer ring — at 40px. The newest mark (the one that just
   landed) carries the one `+$5` chip while it is newest; when the next
   lands, the chip moves with it. Under `prefers-reduced-motion`/still, no
   chip anywhere.
3. **The latest line is one line**: `Wednesday · Coffeeholics · +$5.00`
   (day name, venue, credit) at 14px `--ink-2`, the item name in the
   `aria-label` only. Empty state unchanged.
4. **The foot is two rows**: a 14px `--ink-3` tabular line `$5.99
   membership · $35.00 credit` (both from the model, the credit counting up
   with the loop), then `You're ahead` and the figure as now.

### The receipt (splash, desktop and phone)

1. Rows are one line each: thumbnail, the basket's name, the figure. The
   `Week N` sublines go (the order is the rows'); the week is in the
   `aria-label`.
2. The caption `Then $5 credit, every week you order` goes; the head becomes
   `One month at Coffeeholics · $5 credit a week` in one line (14px 600
   `--ink-3`).
3. The foot as now (label + 28px figure), the membership row 0 as now.

Refused: removing the week labels or the weekday letters (the calendar
stops being a calendar); removing the marks' brand band; any figure not the
model's; text under 14px beyond the weekday letters and the one chip.

### Acceptance

1440 and 1280: object count in the card (elements with a fill, border,
image or text) at least halved against the previous build (report both
numbers); no tile fills on empty days; one chip at most, on the newest
mark; the latest line one line at 480 wide; the foot two rows; the loop
and figures unchanged (−$5.99 → $44.01 on the PR branch). Splash at 375
and 1440: four one-line rows, no caption, the head one line, no overflow,
nothing under 14px but the chip/letters, contrast ≥4.5:1.

### 22.1 — withdrawn

Sam saw the quieter calendar live (dots for empty days, no chips, one-line
latest, two-row foot): "I liked the older version of this to be honest." §22
is withdrawn before it was committed; the calendar stays as §21.1 (filled
day tiles, every mark with its `+$5` chip, the item named in the latest
line, the three-row foot) and the receipt as §20.

---

## 23. Checkout page 0 — the rest, touched up (23 Sep, late)

Sam, on the desktop checkout: "I like the early bird pop up, and the
'every week at every location' thing. But I think the rest could use some
touching up." His screenshot also shows the left column clipped at the
sheet's left edge at roughly a 1,030px pane.

### Keep, untouched

The plan tiles (sold-out / Early-ish Bird / Standard) and the included
panel.

### The schedule, quieter

Five rows each carrying a label, a figure and a note under it (fifteen text
objects; the credit row is gone at $5.99 anyway). Four rows, one line each,
the clause after the label at the same size in `--ink-3`, the figure right:
- `Today` · `$5.99` (the deposit; the tile above already says it counts
  toward the first month — no note)
- `Halloween 2026 · then $5.99 a month, automatic` · `$5.99 a month`
- `Always · never goes up while you stay` · `$5.99`
- `Before we open · no reason needed` · `Full refund`
Every figure from the model as now (`plan.price`, `plan.per`,
`launchWindow`, the refund row). 14px labels and clauses, 16px 700 tabular
figures, hairlines between, `--s-sm` row padding on desktop.

### The button and its count

`Checkout · $5.99 deposit` as now; under it ONE 14px line, centred:
`41 of 100 spots left · closes the end of September` (the tier is named in
the tile above; `seatsLeft`, `SEAT_CAP`, `foundingCloses`), then the meter
at the button's width. Two objects, not three.

### The card

- The caption `Yours from Halloween 2026` goes (the card's STARTS row says
  it); the hairline above the card stays.
- The facts row does not wrap: `PLAN Early-ish Bird` and `STARTS Halloween
  2026` on one line each (`white-space: nowrap` on the values; if 340 is
  too narrow for both at the card's type, the card goes to 360 on desktop
  only).

### The clip

At every width from 1024 to 1200 the sheet's left column must start inside
the viewport: measure `.rs-pay-col`'s left ≥ the sheet's left ≥ 0 and
`scrollWidth === clientWidth` on the sheet and the page. If the two-column
grid's min-content (the tiles' long lines, the ledger's clauses) pushes the
sheet past `100vw − 64px`, set `min-width: 0` on both columns and
`minmax(0, 1fr)` tracks; report the numbers before and after.

### Acceptance

1024, 1100, 1180×820, 1280×800, 1440×900: no clip, no scroll inside the
sheet, the four ledger rows one line each, the count one line, the card's
facts one line each, nothing under 14px but the chips, contrast ≥4.5:1,
the paged steps and Back, the scrim close. 375: one column, review order,
the same four rows and one-line count.

---

## 24. Coffeeholics hero — the photograph is the hero, at every width (23 Sep, late)

Sam: "the bottom half of this page is great, but the hero section needs
some work again. Use /impeccable redesign." Asked which: "Both."

### Diagnosis

Desktop: two columns of equal weight (copy | photograph card) with the
receipt as a third object on top, covering the cups that are the point of
the photograph; two ledgers of figures in one view (the strip's `$5 / 15% /
1×` and the receipt's `$5.99 / +$5.00 / $14.01`). Phone: photo band, lockup,
H1, lead, strip, try link, the still receipt — about 1,300px before the
cards, and the strip says what the three cards under it say again.

### The redesign (replaces; product truth kept)

The splash takes the pitch's structure — **a dark photographic header over
a light body** — so the two pages are one family.

**The band (both widths):** the merchant photograph full-bleed as the
hero's ground — the warm hand-and-cup shot (`/shots/coffeeholicsva-cup.jpg`,
`object-position` so the cup sits centre-right at desktop and centre on a
phone), the venue-card veil deep at the foot (`rgba(18,10,14,.86)` at
100% → `.30` at 45% → `.14` at 0%). The announce bar stays above it.
- Over it, top-left: the collar + `Coffeeholics` + `Coffee · Draper Road`
  in white (as the desktop band has now). Top-right: the lockup + PLUS in
  white.
- Bottom-left, the copy in white: H1 `.t-hero` (36 phone / 56 desktop,
  `$5 credit every week at Coffeeholics`, max 14ch), the lead at 17/18 in
  white .86, then the actions: the filled button (white ground, ink text —
  `--card` on `--ink-1` — since maroon-on-photo is two brands fighting) and
  `Try the first two free today ›` as a white text link beside it (phone:
  under it).
- **Desktop only, bottom-right over the photograph:** the receipt (as §20
  left it, then §22's quieter rows), 340 wide, 24px insets; the photograph
  is 1440 wide so the receipt sits over the counter, not the cups.
- Band height: phone `min(88vh, 640px)`; desktop 640.

**Under the band, on the light ground:**
1. The figure strip (`$5 / 15% / 1×` with its qualifiers) as the first
   block, left-set to the grid, `--s-2xl` under the band — the claim
   stated once, in figures, before the proof.
2. `What you'd save here` and the three cards, as they are.
3. The membership panel, the also-at row, powered-by, legal: as they are.
No receipt on a phone anywhere (the cards are the proof there).

**Motion:** the receipt's loop as now, desktop only. Nothing else moves.
The sticky pair below 1024 keeps its rule (out while the band's own
actions are on screen).

Refused: the strip over the photograph; maroon-filled buttons over the
photograph; a second photograph; any figure not the model's; text under
14px but the chip and the PLUS flag; the pitch's own hero changing.

### Acceptance

375: the band ≤ 640 tall with the H1, lead and both actions inside it and
readable (text-hidden brightest-pixel contrast ≥4.5:1 on every node over
the photo), then the strip, then the cards; page length shorter than
before (report both); no `.ml` on the page. 1440×900: band 640, copy
bottom-left, receipt bottom-right with 24px insets and not over the cup,
the strip under the band, the cards below; the hero's actions inside the
first 900px; contrast as above; the loop as before; reduced motion still.
`/` untouched (fingerprint the pitch hero).

### 22.1 — withdrawn

Sam saw the quieter calendar live (dots for empty days, no chips, one-line
latest, two-row foot): "I liked the older version of this to be honest." §22
is withdrawn before it was committed; the calendar stays as §21.1 (filled
day tiles, every mark with its `+$5` chip, the item named in the latest
line, the three-row foot) and the receipt as §20.

---

## 25. The card's flight from the deck to the checkout (23 Sep, late)

Sam: "refine the animation from the how it works slides to the checkout for
this membership card? I think we'd want the cards to be the same and there
shouldn't be a jolt when it lands in its place at checkout — right now it
looks like the card changes when it lands."

### Why it jolts (read from `src/shell/cardFlight.ts`)

1. **Two different cards.** The deck's last slide shows the card at one size
   and the checkout at another (340, and 420 before §9 fixed it), so the
   flight has to scale, and it scales **non-uniformly** (`scale(sx, sy)` from
   the two rects' ratios). A card whose proportions change in flight is a
   card that "changes".
2. **The clone is the destination's DOM, not the origin's.** At takeoff the
   deck's card is swapped for a clone of the checkout's card squeezed to the
   deck's rect — any difference in type, padding or shadow between the two
   renderings pops at frame one.
3. **The landing rect is measured too early.** `to` is read in the mount
   effect while the checkout sheet may still be arriving (`sheetUp` /
   `rsPopIn`) and before fonts and the two-column grid settle, so the clone
   lands where the card *was*, and the 200ms crossfade reveals the real card
   somewhere slightly else — the jolt.

### The refinement

- **One card.** `TapInCard` at one width on both surfaces: 340 on the deck's
  slide 9 and at the checkout's close, phone and desktop (`min(340px,
  100% − 48px)` below 480). Same class, same shadow, same radius, same name
  field, so the flight is a **pure translate**, no scale.
- **Move the real card, not a clone.** FLIP on the destination card itself:
  after the sheet is still (`is-still` set for a flight, so the sheet does
  not animate its own entrance under the flight), after `document.fonts.ready`
  and two animation frames, measure `to`, apply `transform:
  translate(dx, dy)` from the stashed origin, and animate to `none` over
  820ms on the house expo-out. No clone, no crossfade, nothing to swap at
  landing. The stashed origin is measured with the deck's float/drift
  animations cancelled, as now.
- **Hold the rest back, as now** (`data-flight="in"` brings the sheet's
  other content up after the card lands); keep the reduced-motion bypass.
- If the origin rect is off-screen or the sizes still differ by more than
  2px (a resize between routes), skip the flight rather than scale.

### Acceptance

Desktop 1440 and phone 375: the deck's card and the checkout's card measure
the same width and height (report both); during the flight the card's box
keeps its size (sample the animated rect at 3 points); at landing the card's
final rect equals its settled rect with no second movement (compare the
rect at `finished` to the rect 500ms later); no clone in the DOM; reduced
motion still opens the sheet without a flight; the deck's slide 9 and the
checkout otherwise unchanged.

---

## 26. Checkout page 0 — the schedule as a timeline, the card in its home (23 Sep, late)

Sam, after §23 was queued: "were you going to /impeccable redesign the
placement of these two [the schedule + button block, and the card], and
the design of the first screenshot [the schedule]?" §23 polished; this
replaces.

### The schedule is a timeline, not a table

A ledger of five label/figure/note rows reads as terms. What it describes
is three moments in time and one promise, so it is drawn as one:
- A vertical rail at the left (2px `--hair-lit`) with three 10px dots
  (`--ink-1` filled for Today, `--card` with a 2px `--ink-2` ring for the
  two to come), 24px from the text.
- Three stops, each two lines: the moment 14px 600 `--ink-2` (`Today`,
  `Halloween 2026`, `Always`) with the figure right-aligned 16px 700
  tabular `--ink-1` on the same line (`$5.99`, `$5.99 a month`, `$5.99`),
  and under it the clause 14px `--ink-3` (`Early-ish Bird deposit, counts
  toward your first month` · `then automatically, your first month
  already paid` · `never goes up while you stay a member`). 20px between
  stops.
- Under the rail, one sentence 14px `--ink-3`: `Full refund any time before
  we open, no reason needed.` (the refund charge row's own words).
- Then the button `Checkout · $5.99 deposit`, then §23's one-line count
  and the meter.
All strings from the model as §16/§23 source them; the tier name from
`foundingTierName`.

### The card lives in the included panel

The right column is one object: the included panel grows a foot. After
the guarantee line, a hairline, then the card centred (340) with 20px
above and 4px below inside the panel's padding, and no caption (the card
says STARTS itself). The panel's heading stays; the card is what the
heading's list adds up to. On a phone the same panel closes the page-0
column, so the card is inside the panel there too (no separate
`.rs-card-close` block).

### Desktop balance

Left: tiles, timeline, refund line, button, count. Right: the panel with
the card inside. The two columns' bottoms within 48px at 1180×820 and
1440×900 (report), no scroll inside the sheet at 1024–1440.

Refused: a struck price anywhere but Kiran's tile; notes under the button;
a second panel; any figure not the model's; text under 14px but the chips.

### Acceptance

As §23's, plus: three dots on one rail, the stops' figures on one right
edge, the refund sentence one line at desktop (two at 375 is fine), the
card inside the panel at every width with no caption, the paged steps and
the scrim close intact, the flight (§25) landing on the card in its new
place.

---

## 27. The count under the button — a share in the line, not a second bar (23 Sep, late)

Sam, with a screenshot of the button, the count and the meter: "also we
need to /impeccable redesign this section of checkout."

### What the screenshot shows

Three objects stacked at one measure: the maroon button, a centred
sentence, and a 4px bar filled 59% in `--ink-1`. Two bars of the same
width, one under the other, and the lower one reads as the upper one's
progress — a button that is 59% loaded. The bar says "59" while the line
says "41", and the reader has to reconcile them; §23 kept that on purpose
(a bar nearly full reads as a room nearly full), but a bar's *first*
reading, directly under a control, is loading. And the sentence, 14px 500
`--ink-2` with one bold number, is a caption dressed as a statement, so the
block has three voices where the page wants one action and one reason.

### The redesign

The block is two objects, not three: the button, and one line under it
that carries the share as a glyph instead of as a rule.

- **The meter becomes a 16px pie at the head of the line.** A disc of
  `--hair-lit` with the seats taken drawn as a wedge from twelve o'clock,
  clockwise, in `--ink-1`: CSS only, `background: conic-gradient(var(--ink-1)
  calc(var(--p) * 1turn), var(--hair-lit) 0)`, `border-radius: 50%`,
  `aria-hidden`, the same `--p = 1 − left / cap` the bar used (Reserve.tsx's
  warning about WHICH fraction stands). A pie is read as "how full" — the
  reading the bar was trying to earn without being a bar — and it belongs
  to the timeline's family above it: the dots mark moments, this one marks
  a share.
- **The line is one sentence, quieter.** `41 of 100 spots left · closes at
  the end of September`: 14px 500 `--ink-3`, the number 700 tabular
  `--ink-1` as now; the rest of the line steps down a rung so the button is
  the only loud thing. The template gains the word "at" (the model's
  `foundingCloses` phrase is untouched). The pie and the text are one
  centred unit (`inline-flex`, 8px gap), the pie aligned to the first
  line's centre, not the block's.
- **Phone.** Below 480 the middle dot is dropped and the deadline takes its
  own line: two centred lines, the pie leading the first. At 1024 and up
  the sentence is one line.
- **Spacing.** `--s-md` between the button and the line, as now; nothing
  else under the button. The docked twin, the `.pay-slot` observer, the
  `FOUNDING_OPEN` gate (the block does not render at zero) and every source
  (`seatsLeft()`, `SEAT_CAP`, `foundingCloses`) are unchanged.

Copy, for Sam: "closes Sep 30" is shorter and firmer than "closes at the
end of September" — one edit to `foundingCloses` in content.ts, his call,
not made here.

Refused: a second bar at the button's measure; segments or blocks (Sam,
15 Sep: "a single line, not little blocks"); a countdown, a clock or a
"selling fast"; red or maroon anywhere on the pie; the count inside the
button; a plate or card around the line; any number the model does not
return.

### Acceptance

375, 1180×820, 1440×900: no `.rs-meter` in the DOM; the pie is 16×16 with
a computed wedge of `0.59turn` (report the computed `background`); the
sentence is one line at ≥1024 and two centred lines at 375 (report line
counts); `--ink-3` on the panel ground measures ≥ 4.5:1 (report the pair);
the docked button still hides while the slot is on screen; §26's column
bottoms and no-scroll figures still hold (report); nothing animates that
did not before.

---

## 28. The money column — icons on the rail, the pay card (23 Sep, late)

Sam, with two screenshots (the §26 timeline; the §27 button and count):
"this looks worse than what we had before, but I like the idea of the
timeline — can you use /ui-ux-pro-max to redesign the screenshotted
sections. For the second screenshot it's still not great, needs to feel
like that's a part of a parent container that holds the two together."
Three directions were drawn with the real tokens and Gilroy (A: icons on
the rail and a pay card; B: the same inside one order panel mirroring the
right column; C: chips on the rail, one line a stop). This is A; B and C
are held.

### Why §26's timeline read worse than the ledger

Ten-pixel dots on a two-pixel grey rail is the form-wizard stepper, not a
schedule. The moments were 14px `--ink-2`, smaller and lighter than the
figures beside them, so each row led with money; and the clause under
every stop made six lines where the ledger had four. The design database
has no entry for a deferred-charge timeline; the references that read well
(Strava, Mesh, Deepstash: "how your free trial works") share one shape:
a 28–40px icon marker, a bold moment, a quiet clause, a hairline
connector, and the charge stated once at the end.

### The timeline, redrawn

- A heading above it, `What you pay, and when` (`.t-title`, the list's
  own aria-label today), so the left column names itself as the right one
  does.
- Three stops. Each has a 32px round marker — `--card` fill, 1px
  `--hair-lit` ring, `--rest` shadow — holding a 16px icon drawn at a
  1.75 stroke in `--ink-1`: a banknote for Today (`<rect x="3" y="6"
  width="18" height="12" rx="2"/><circle cx="12" cy="12" r="2.5"/>`), a
  calendar for Halloween 2026 (`<rect x="3" y="5" width="18" height="16"
  rx="2"/><path d="M3 10h18M8 3v4M16 3v4"/>`), a lock for Always (`<rect
  x="5" y="11" width="14" height="10" rx="2"/><path d="M8 11V8a4 4 0 0 1
  8 0v3"/>`). Today's marker is filled `--ink-1` with the icon in `--card`:
  the present moment. A 1px `--hair-lit` connector runs marker to marker
  on their centre line. 14px from marker to text, 18px between stops.
- The moment is 15px 700 `--ink-1` with the figure right-aligned on the
  same 20px line, 15px 700 tabular `--ink-1` (`$5.99`, `$5.99 a month`,
  `$5.99`). The clause under it is 14px 500 `--ink-3`, capitalised as a
  line of its own: `Early-ish Bird deposit, counts toward your first
  month` · `Then automatically, your first month already paid` · `Never
  goes up while you stay a member`. The tier name from
  `foundingTierName`; every figure from the model; the credit stop keeps
  its gate and, if it ever renders, is a fourth stop with the banknote.
- The refund is one 14px `--ink-3` line led by a 16px counter-clockwise
  arrow in the same ink (`<path d="M3 12a9 9 0 1 0 3-6.7"/><path d="M3
  4v5h5"/>`): `Full refund any time before we open, no reason needed.`

### The pay card

The button and the count are one object. A `--card` plate at `--r-card`,
1px `--hair` border, `--rest` shadow, 12px padding, holding the button at
full width and, 10px under it, the count centred: §27's 16px pie (the
seats taken as an `--ink-1` wedge on `--hair-lit`) then `41 of 100 spots
left · closes at the end of September`, 14px 500 `--ink-3`, the number
700 `--ink-1`; below 480 the deadline takes its own centred line. The
card wraps `.pay-slot` (the observer's box stays the button's), the
docked twin is unchanged, and there is no meter and no second bar.

### Desktop balance

The two columns' bottoms within 48px at 1180×820 and 1440×900, no scroll
inside the sheet from 1024 to 1440. The right column stretches as §26
left it, so the air above the card in the included panel absorbs the
difference; report the figures.

Refused: numbered or lettered markers; maroon on a marker; a meter; a
plate around the timeline (that is B, held); text under 14px but the
chips; any figure the model does not return.

### Acceptance

375, 1180×820, 1440×900: three 32×32 markers on one vertical axis with a
connector between each pair; the three figures on one right edge; the
pay card's border and shadow computed; the count one line at ≥1024, two
at 375; no `.rs-meter` in the DOM; the §25 flight still lands still on
the card in the panel; §26's no-scroll and column-bottom figures hold
(report); `tsc`, `guards.py` and the detector clean.
