# Landing `polish/landing-pages` on Kiran's `main` — 23 Sep 2026

Planned by Fable 5.1; executed by an Opus 5 worker; verified by Fable.
Sam: "plan rebase to kiran's main with Fable, and implement with opus."

## The two sides

- **Ours:** `polish/landing-pages`, 47 commits since `5e4dd35` (`purple dot`,
  the tip of `main` on 20 Sep), 33 files, +6,406/−2,039. Every commit is
  pushed; the branch is the record. The plan it executed is
  `docs/POLISH-2026-09-21.md` §1–§21.
- **Kiran's:** `main` moved by exactly one commit, `a703960` (23 Sep,
  "price change/second chance"):
  1. **The price is $5.99.** `FOUNDING_MONTHLY` 4.99 → 5.99 (`content.ts`),
     `FOUNDING_MONTHLY_USD` (`savings.ts`), `FIRST_INVOICE_TOTAL_CENTS`
     499 → 599 (`SubscriptionPayButton.tsx`), 599 added to `Membership.tsx`'s
     accepted-cents list. New export `firstEarlyBirdPrice = usd(4.99)`,
     display only.
  2. **A sold-out tile.** On `/reserve`, above the live tile: `Early Bird
     Deposit`, `$4.99` struck, corner flag `Sold out`, refused on tap the way
     Standard is; `refused` becomes `"sold" | "later" | null`; the live tile is
     relabelled **"Early-ish Bird Deposit"**; the refusal status line gains a
     sold-out sentence.
  3. **The sign-in scrim goes transparent.** `.cs-scrim` loses its dim and
     its 3px blur ("read as a black overlay under the sign-in popup"); the
     `.reserve-layer .cs-scrim` override is removed; `.plan-opt.is-sold`
     rules added.

Files on both sides: `src/model/content.ts`, `src/routes/Reserve.tsx`,
`src/styles/reserve.css`. Ours rewrote `Reserve.tsx` and `reserve.css` six
times (§8, §9, §13, §15, §16, §18), so a commit-by-commit rebase would
resolve the same conflict six times over. That is the wrong tool.

## The shape: one squash on top of `main`

1. New branch `polish/landing-pages-rebased` from `kiran/main` (`a703960`).
2. `git merge --squash polish/landing-pages` — the whole diff, one conflict
   pass, resolved semantically (below). One commit, whose message is the
   plan's section list; the PR links the 47-commit branch as the record.
   This is the shape Sam chose for PR #33 ("one commit on main instead of
   32"); it breaks Kiran's <10-files rule knowingly, and the PR says so.
3. The calendar (§21) lands on `polish/landing-pages` while this runs; it is
   **cherry-picked** onto the rebased branch afterwards (a second squash
   would re-apply the whole diff).
4. One PR into `main`, squash-merged, so `main` gets one commit.

## Conflict resolution, decided here

| where | take | because |
|---|---|---|
| `content.ts` `FOUNDING_MONTHLY`, `firstEarlyBirdPrice`; `savings.ts`; `SubscriptionPayButton.tsx`; `Membership.tsx` | **Kiran's** | the price is his call; every surface of ours derives from these constants |
| `Reserve.tsx` structure (review sheet, paged flow, included panel, two columns) | **ours** | §16/§18 replace the file; his change is a tile, not a layout |
| `Reserve.tsx` sold-out tile + `refused: "sold"\|"later"\|null` + `saySoldOut` + the status sentences + the "Early-ish Bird Deposit" label | **Kiran's, re-applied** into our `plan-pick` block above the live tile, with our `is-refused` flash | his product change, on our structure |
| `reserve.css` `.plan-opt.is-sold` rules | **Kiran's** | new rules, no overlap |
| `reserve.css` `.cs-scrim` dim/blur | **ours (6px blur, dimmed)** — and the PR says why | Sam, 22 Sep: "whenever a pop-up or a modal is triggered, blur the background." Kiran's black-overlay complaint was the old second sheet stacked under the sign-in; §9 removed that sheet, so the case he fixed no longer exists |
| `.reserve-layer .cs-scrim` override | gone on both sides | — |

## What the price change does to our surfaces (derived, verify each)

Every figure of ours reads `monthlyToday` / `FOUNDING_MONTHLY` / `BENEFIT`,
so at $5.99:
- Hero prices, tiles, the docked bar label, the splash band: **$5.99**.
- The earn-back gate `FOUNDING_OPEN && monthlyToday < BENEFIT.creditUsd` is
  now **false** (5.99 ≥ 5): the checkout's `Your first $10+ order · +$5
  credit · more than the deposit` ledger row does not render. Correct — the
  claim is no longer true. Nothing else may still say "earn it back".
- The Coffeeholics ledger and the pitch's calendar start at **−$5.99** and
  end at **$14.01** / **$44.01**; `campaignLedger` hides itself only if the
  credits stop covering the fee (they do not).
- The savings slider's `FOUNDING_MONTHLY_USD` nets against 5.99.
- Seat copy stays "Early Bird spots" — Kiran renamed only the tile. The
  "Early-ish Bird" label is his wording; leave it on the tile and nowhere
  else; the PR asks whether the seat line and the receipt should follow.
- `SubscriptionPayButton`'s 599 and Kiran's own note: the Stripe price ids
  must move to a 599 price before deploy. Not ours; the PR repeats it.

## Verification (the worker, then Fable)

`npx tsc --noEmit -p .`, `python3 scripts/guards.py`, the impeccable
detector on the three resolved files; then at 375 and 1440: `/` (hero
price $5.99, the calendar's foot from −$5.99 to $44.01, everything else as
§14/§19/§21), `/coffeeholics` (band $5.99, ledger −$5.99 → $14.01),
`/reserve` (sold-out $4.99 tile struck and flagged above the live
"Early-ish Bird Deposit" $5.99 tile, both refusals flash, no earn-back row,
the schedule's other rows, the paged steps, the scrim close, the docked
bar `Checkout · $5.99 deposit`), the sign-in sheet's scrim blurred. Then
`git diff kiran/main..polish/landing-pages-rebased --stat` equals our
33-file diff plus Kiran's six files, and `git log --oneline kiran/main..`
is one commit (two once the calendar is picked).

## Not in this plan

- Closing PRs #3–#32 (superseded by #33): Sam has not answered; offered.
- Deploying: Vercel CLI, `plus.tapin.app`, the Stripe 599 price ids — Sam's
  and Kiran's.
